<p id="notice"><%= notice %></p>

<h1>Listing Comment2s</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>Post</th>
      <th>Something</th>
      <th>Somethingelse</th>
      <th>Something</th>
      <th>More</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comment2s.each do |comment2| %>
      <tr>
        <td><%= comment2.content %></td>
        <td><%= comment2.post %></td>
        <td><%= comment2.something %></td>
        <td><%= comment2.somethingelse %></td>
        <td><%= comment2.something %></td>
        <td><%= comment2.more %></td>
        <td><%= link_to 'Show', comment2 %></td>
        <td><%= link_to 'Edit', edit_comment2_path(comment2) %></td>
        <td><%= link_to 'Destroy', comment2, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Comment2', new_comment2_path %>
